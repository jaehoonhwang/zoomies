// TODO(jaehoon): Implement store update on url changes
chrome.tabs.onZoomChange.addListener(function(zoomChangeInfo) {
  //let newZoomChange = zoomChangeInfo.newZoomFactor;
  //let oldZoomChange = zoomChangeInfo.oldZoomFactor;
  //let tabId = zoomChangeInfo.tabId;
}
);


function updateZoomLevel(tabId: number) { }
